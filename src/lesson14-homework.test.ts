import { describe, it, expect } from '@jest/globals';

import { excerciseA, excerciseB, excerciseC } from './lesson14-homework';

describe('excerciseA', () => {
  it('should be defined', () => {
    expect(excerciseA).toBeDefined();
  });
});

describe('excerciseB', () => {
  it('should be defined', () => {
    expect(excerciseB).toBeDefined();
  });
});

describe('excerciseC', () => {
  it('should be defined', () => {
    expect(excerciseC).toBeDefined();
  });
});
